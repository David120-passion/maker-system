<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出（可选） -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/maker-system.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/maker-system.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 根日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- 策略层日志：DEBUG 级别，方便调试 -->
    <logger name="com.xinyue.maker.strategy" level="DEBUG" />
    
    <!-- 核心层日志：INFO 级别 -->
    <logger name="com.xinyue.maker.core" level="INFO" />
    
    <!-- 接入层日志：INFO 级别 -->
    <logger name="com.xinyue.maker.io" level="INFO" />
    
    <!-- 测试事件调度器：DEBUG 级别 -->
    <logger name="com.xinyue.maker.core.TestEventScheduler" level="DEBUG" />
</configuration>

